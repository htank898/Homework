let pizzaPlace;
let numberOfToppings;

pizzaPlace = "Harrison's Pizza";
numberOfToppings = 12;

// Logging variables and type of variables to console.
console.log("The name of my pizza place is " + pizzaPlace, typeof pizzaPlace, numberOfToppings, typeof numberOfToppings);

const templiteral = `Hello, welcome to ${pizzaPlace}, we serve ${numberOfToppings} toppings.`;

console.log(templiteral);

if (numberOfToppings < 10) {
  console.log("Quality, not Quantity");
} else {
  console.log("A whole lot of Pizza");
}

//bonus

for(let i = numberOfToppings; <=)
i+++
